# Copyright (c) 2023-2026 Deephaven Data Labs and Patent Pending

# Run benchmarks on a remote system according to the release defined 
# in resources/release-benchmark-docker-compose.yml
# - Calls the reusable worflow remote-benchmarks.yml

name: Release Benchmark Test on Docker Deephaven

on:
  workflow_dispatch:
    inputs:
     docker_image:
       description: 'Docker Image Name'
       required: true
       default: '0.36.0'
       type: string

jobs:
  validate-inputs:
    runs-on: ubuntu-24.04
    steps:
    - name: Validate Docker Image Version
      run: |
        if ! [[ "${{ inputs.docker_image }}" =~ ^[0-9]+(\.[0-9]+){1,2}$ ]]; then
          echo "::error::docker_image '${{ inputs.docker_image }}' is not a valid version (expected numeric format like 0.36.0 or 42.4)"
          exit 1
        fi

  process-release-benchmarks:
    needs: validate-inputs
    uses: ./.github/workflows/remote-benchmarks.yml
    with:
      run_type: release
      docker_image: ${{ inputs.docker_image }}
      run_label: "<version>"
      test_package: "io.deephaven.benchmark.tests.standard"
      test_class_list: "*"
      test_iterations: 5
      scale_row_count: 10000000
      distribution: random
      test_device_addr: "<default>"
      config_options: "<default>"
    secrets: inherit
